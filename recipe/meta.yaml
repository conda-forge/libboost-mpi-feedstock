{% set boost_cpp_version = boost_cpp_version|default("1.78.0") %}

package:
  name: libboost-mpi
  version: {{ boost_cpp_version }}

source:
  url: https://boostorg.jfrog.io/artifactory/main/release/{{ boost_cpp_version }}/source/boost_{{ boost_cpp_version|replace(".", "_") }}.tar.bz2
  sha256: 8681f175d4bdb26c52222665793eef08490d7758529330f98d3b29dd0735bccc
  patches:
    - 5eff1ecc8413b0dc93a1ab047d7fed751e6cb40e.patch  # [not win]

build:
  number: 2

requirements:
  build:
    - {{ compiler('cxx') }}

  host:
    - boost-cpp
    - {{ mpi }}

  run:
    - boost-cpp
    - {{ mpi }}

  run_constrained:
    - libboost <0

test:
  commands:
    # Verify library
    - test ! -f $PREFIX/lib/libboost_mpi.a                                               # [unix]
    - test -f $PREFIX/lib/libboost_mpi.dylib                                             # [osx]
    - test -f $PREFIX/lib/libboost_mpi.so                                                # [linux]
    - if not exist %PREFIX%\\Library\\bin\\boost_mpi.dll exit 1                          # [win]
    - if not exist %PREFIX%\\Library\\lib\\boost_mpi.lib exit 1                          # [win]
    - if exist %PREFIX%\\Library\\lib\\libboost_mpi.lib exit 1                           # [win]

about:
  home: http://www.boost.org/
  license: BSL-1.0
  license_file: LICENSE_1_0.txt
  summary: Free peer-reviewed portable C++ source libraries.

extra:
  recipe-maintainers:
    - sdebionne
